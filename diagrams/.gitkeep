Exported SVG/PNG diagram assets live here.

Mermaid source stays inline in the chapter Markdown so it is versioned
with the prose it explains. This directory holds only the pre-rendered
exports that EPUB and print builds need, generated by the build, not
edited by hand.
